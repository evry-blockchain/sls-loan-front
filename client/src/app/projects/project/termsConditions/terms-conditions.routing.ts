
import { Routes } from '@angular/router';
import { TermsConditionsComponent } from "./terms-conditions.component";

const router: Routes = [
  {
    path: 'terms-and-conditions',
    component: TermsConditionsComponent
  }
];

export const termsConditions = router;
